import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `    
    <!-- Router Outlet -->
    <router-outlet></router-outlet>
  `,
})

// App Component class
export class AppComponent {}
